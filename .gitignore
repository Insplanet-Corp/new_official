# dependencies
node_modules
.pnp
.pnp.*
.yarn/*

# next.js
.next
# 검증용 빌드 디렉터리(BUILD_DIR=…)까지 전부. 커밋되면 tsconfig 의 include 가
# 낡은 validator.ts 를 잡아 next build 는 통과하는데 typecheck 만 깨진다.
.next-*
out
build
next-env.d.ts

# production / deploy
.vercel

# env — 절대 커밋하지 말 것 (.env.example 만 커밋)
.env
.env.*
!.env.example

# 인증서 / 키
*.pem
*.key
*.p12
*.keystore

# typescript
*.tsbuildinfo

# 로그
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# 에디터 / OS
.DS_Store
Thumbs.db
.idea
.vscode
*.swp

# claude code (로컬 설정만 제외, 공유 설정은 커밋)
.claude/settings.local.json

# Supabase CLI 가 만드는 작업용 폴더 (project-ref 캐시 등)
supabase/.temp/
